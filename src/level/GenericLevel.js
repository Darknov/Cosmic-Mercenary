class GenericLevel {
  constructor(width, height, backgroundName, type = 'generic') {
    this.width = width;
    this.height = height;
    this.backgroundName = backgroundName;
    this.type = type;
  }
}